@{
    Layout = "_Layout";
}

@using Factory.Models

<div class="Macines">
    @if(@Model.Count == 0)
    {
        <h3>No machines added!</h3>
    }
    else
    {
        <ul class="list-group">
            @foreach(Machine machine in Model)
            {
                <li class="list-group-item">@Html.ActionLink($"{machine.Name}", "Details", new {id = machine.MachineId })</li>
            }
        </ul>
    }
</div>
<br>
@Html.ActionLink("Add new Machine", "Create")</p>
@Html.ActionLink("Return Home", "Index", "Home")</p>
